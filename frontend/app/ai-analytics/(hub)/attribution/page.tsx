"use client";

import ComingSoonScreen from "@/components/evaluation/ComingSoonScreen";

export default function AttributionPlaceholderPage() {
  return (
    <ComingSoonScreen
      title="Attribution"
      milestone="M6"
      description="Why your return happened — the full effect waterfall (selection, allocation, timing, execution, funding, overrides) plus sector, regime, and per-holding breakdowns."
    />
  );
}
